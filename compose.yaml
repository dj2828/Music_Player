services:
  music_player:
    image: dj2828/music_player:latest
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 80:80
    volumes:
      - data:/app/data
      - ./import_docker:/app/music
    environment:
      - IP=
      - DOCKER=True
      - YT_FOLDER=/app/data/yt
      - MUSIC_FOLDER=/app/music
      - COVER_FOLDER=/app/data/cover
volumes:
  data: